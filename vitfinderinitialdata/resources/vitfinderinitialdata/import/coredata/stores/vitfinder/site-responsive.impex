#
# Import the Responsive CMS Site configuration for the Vitfinder UK Store
#
$productCatalog=vitfinderProductCatalog
$contentCatalog=vitfinderContentCatalog
$contentCV=catalogVersion(CatalogVersion.catalog(Catalog.id[default=$contentCatalog]),CatalogVersion.version[default=Staged])[default=$contentCatalog:Staged]
$defaultLanguage=en_GB
$storeUid=vitfinder
$siteUid=vitfinder


# CMS Site - Update the theme to be responsive
INSERT_UPDATE CMSSite;uid[unique=true];theme(code);
;$siteUid;blue;